/* string_contains.c


   B. Bird - 05/10/2016
*/

#include <stdio.h>

/* string_contains(big_string, small_string)
   Test whether small_string occurs anywhere in big_string.
   If so, return the index of the character at the beginning
   of the first occurrence of small_string in big_string. 
   Otherwise, return -1.

   For example:
     - string_contains("Hello World", "World") will return 6
     - string_contains("Raspberry", "berry") will return 4
     - string_contains("Raspberry", "Berry") will return -1
   
   Pre-conditions:
     big_string and small_string are valid, null terminated C strings.
   Return value: 
     A non-negative integer if small_string is contained in big_string.
     -1 otherwise
*/
int string_contains(char big_string[], char small_string[]){
	return 0;
}

int main(){
	// Add some test cases for string_contains here...
	return 0;
}
