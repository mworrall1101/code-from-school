/* string_startswith.c


   B. Bird - 05/10/2016
*/

#include <stdio.h>

/* string_startswith(s, prefix)
   Test whether the string s starts with the string prefix.
   If so, return 1. Otherwise, return 0.
   
   Pre-conditions:
     s and prefix are valid, null terminated C strings.
   Return value: 
     1 if string s starts with string prefix.
     0 otherwise
*/
int string_startswith(char s[], char prefix[]){
	return 0;
}

int main(){
	// Add some test cases for string_startswith here...
	return 0;
}
